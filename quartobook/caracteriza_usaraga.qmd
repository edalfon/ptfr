---
title: "Usaragá"
fig-cap-location: top
---

```{r}
#| eval: true
survey <- tar_read(usaraga_survey) |> filter(startsWith(beneficia, "Beneficia"))
```

En el diseño del proyecto fortalecimiento de la cadena productiva del plátano, la población directamente beneficiaria está compuesta por 10 familias. Estas familias residen en las comunidades de Usaragá, Pampón y Puerto Viveros, todas pertenecientes al Consejo Comunitario San Andrés de Usaragá, en el municipio de Bajo Baudó, Chocó. El proyecto estableció que la selección de los beneficiarios se haría con base en criterios específicos, buscando a personas con vocación para la producción de plátano, que fueran del territorio y pertenecieran a familias vulnerables, de escasos recursos y que, en su mayoría, hubieran sufrido la violencia que afectó a la región. En términos de composición, el proyecto estipula que el 100% de los beneficiarios deben ser afrocolombianos o negros, al ser la única etnia que habita en dicho territorio colectivo. Además, el diseño incluye metas de género y edad, planeando que al menos el 30% de los beneficiarios fueran mujeres, todos mayores de edad. También se estableció como objetivo que al menos el 20% de los participantes fueran jóvenes con edades comprendidas entre los 18 y 28 años. @diseno_usaraga

Según el diseño del proyecto la población que se beneficia indirectamente del proyecto en Usaragá es la comunidad en general de las veredas que conforman el Consejo Comunitario San Andrés de Usaragá (TODO: aunque el documento no define explícitamente a los beneficiarios indirectos, y de hecho repite la información de los beneficiarios directos), mediante efectos indirectos como beneficios económico vía mejorares precios del plátano, oportunidades de empleo temporal en los cultivos, entre otros. @diseno_usaraga Adicionalmente, teniendo en cuenta que se desarrolló un plan de formación que no se limitó a los 10 beneficiarios directos, sino que se extendió a la comunidad en general, la cual participó en talleres sobre temas relevantes como género y relaciones interpersonales, los beneficiarios indirectos del proyecto van maás allá de los solo beneficios ecnómisoc y podrían darse por estos otros mecanismos que podríasn generar mayor participación de los habitantes en los espacios comunitarios y motivar a involucrarse en futuras oportunidades. @entrevistas_usaraga

A continuación se presenta una caracterización de los beneficiarios del proyecto, a partir de los datos recopilados en campo durante la encuesta para la evaluación del proyecto.


## Ubicación geográfica

A continuación se presenta la distribución geográfica, del lugar donde se lograron realizar las encuestas de los beneficiarios del proyecto.

```{r}
#| label: map_all
#| fig-cap: Ubicación de entrevista de los beneficiarios
leaflet::leaflet(height = "700px") |>
  leaflet::addProviderTiles(leaflet::providers$OpenStreetMap, group = "Street View") |>
  leaflet::addProviderTiles(leaflet::providers$Esri.WorldImagery, group = "Satellite") |>
  leaflet::addLayersControl(
    baseGroups = c("Street View", "Satellite"),
    options = leaflet::layersControlOptions(collapsed = FALSE)
  ) |>
  # leaflet::addTiles() |> # Add OpenStreetMap tiles
  leaflet::addAwesomeMarkers(
    lng = survey$longitud,
    lat = survey$latitud,
    icon = leaflet::awesomeIcons(
      icon = "user",
      library = "fa",
      markerColor = "lightblue",
      squareMarker = TRUE,
      iconColor = "black",
      spin = FALSE
    ),
    options = leaflet::markerOptions(opacity = 0.7)
  )
```

```{r}
#| label: map_siviru
#| fig-cap: Ubicación de entrevista de los beneficiarios - Sivirú
leaflet::leaflet(height = "700px") |>
  leaflet::addProviderTiles(leaflet::providers$OpenStreetMap, group = "Street View") |>
  leaflet::addProviderTiles(leaflet::providers$Esri.WorldImagery, group = "Satellite") |>
  leaflet::addLayersControl(
    baseGroups = c("Street View", "Satellite"),
    options = leaflet::layersControlOptions(collapsed = FALSE)
  ) |>
  # leaflet::addTiles() |> # Add OpenStreetMap tiles
  leaflet::addAwesomeMarkers(
    lng = survey |> filter(latitud <= 4.950081) |> pull(longitud),
    lat = survey |> filter(latitud <= 4.950081) |> pull(latitud),
    icon = leaflet::awesomeIcons(
      icon = "user",
      library = "fa",
      markerColor = "lightblue",
      squareMarker = TRUE,
      iconColor = "black",
      spin = FALSE
    ),
    options = leaflet::markerOptions(opacity = 0.7)
  )
```

```{r}
#| label: map_dotenedo
#| fig-cap: Ubicación de entrevista de los beneficiarios - Dotenedó
leaflet::leaflet(height = "700px") |>
  leaflet::addProviderTiles(leaflet::providers$OpenStreetMap, group = "Street View") |>
  leaflet::addProviderTiles(leaflet::providers$Esri.WorldImagery, group = "Satellite") |>
  leaflet::addLayersControl(
    baseGroups = c("Street View", "Satellite"),
    options = leaflet::layersControlOptions(collapsed = FALSE)
  ) |>
  # leaflet::addTiles() |> # Add OpenStreetMap tiles
  leaflet::addAwesomeMarkers(
    lng = survey |> filter(latitud >= 4.950081) |> pull(longitud),
    lat = survey |> filter(latitud >= 4.950081) |> pull(latitud),
    icon = leaflet::awesomeIcons(
      icon = "user",
      library = "fa",
      markerColor = "lightblue",
      squareMarker = TRUE,
      iconColor = "black",
      spin = FALSE
    ),
    options = leaflet::markerOptions(opacity = 0.7)
  )
```


## Edad y sexo asignado al nacer

Las gráficas de edad y sexo de los beneficiarios de Usaragá muestran una población principalmente adulta, con concentración entre los 40 y 60 años, lo que refleja la participación de personas en edad productiva y con experiencia comunitaria. En cuanto al sexo, se observa una mayor proporción de hombres (66,7%) frente a mujeres (33,3%). Esta composición, si bien está en línea con los criterios definidos para el proyecto, resalta las dinámicas de género diferenciadas en la comunidad.

```{r}
#| label: edad
#| fig-cap: Edad
ggplot(survey) +
  aes(x = edad) +
  geom_histogram(bins = 10, fill = "steelblue", color = "white", alpha = 0.7) +
  geom_rug() +
  labs(y = "Frecuencia")
```

```{r}
#| tbl-cap: Cuantiles
quantile(survey$edad, na.rm = TRUE) |>
  t() |>
  knitr::kable()
```


```{r}
#| label: sexo
#| fig-cap: Sexo
ggplot(survey) +
  aes(x = sexo) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```


## Orientación sexual e identidad de género

Las gráficas sobre orientación sexual e identidad de género en Usaragá muestran que la mayoría de las personas beneficiarias (66,7%) manifestó sentir atracción hacia mujeres, mientras que el 33,3% reportó atracción hacia hombres. En cuanto al reconocimiento de género, el 55,6% se identifica como hombres y el 44,4% como mujeres.

TODO: dejar los mosaic plots?, y en general resaltar los cruces?, o no?
TODO: validar campo, el que cambia e lporcentaje de identidad de género

```{r}
#| label: orientacion_sexual
#| fig-cap: Orientación sexual
ggplot(survey) +
  aes(x = orientacion_sexual) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia")
```


```{r}
#| label: identidad_genero
#| fig-cap: Identidad de género
ggplot(survey) +
  aes(x = identidad_genero) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia")
```

## Autoreconocimiento étnico

El 100% de las personas beneficiarias en Usaragá se reconoce como negra, afrodescendiente o afrocolombiana. Este resultado es consistente con la composición étnica del consejo comunitario de San Andrés de Usaragá, donde la identidad afro es un elemento central de la vida social, cultural y productiva. Es igualmente alineado con la definición de la población objetivo del proyecto.

```{r}
#| label: etnia
#| fig-cap: Autoreconocimiento étnico
ggplot(survey) +
  aes(y = etnia |> forcats::fct_infreq() |> forcats::fct_rev()) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent_h() +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1),
    labels = \(x) stringr::str_wrap(x, 20)
  ) +
  labs(x = "Frecuencia", y = NULL) # title = attr(survey$etnia, "label")
```

## Educación

La gran mayoría de los beneficiarios (88,9%) sabe leer y escribir, aunque persiste un 11,1% que no. En cuanto al logro educativo, predomina ampliamente la básica primaria (77,8%), seguida por quienes alcanzaron la educación media (11,1%), mientras que otro 11,1% no cuenta con ningún nivel educativo formal. No se registran casos de formación técnica, tecnológica o universitaria. Estos resultados reflejan una población con acceso limitado a niveles educativos superiores, lo cual coincide con la definición de la población objetivo del proyecto, centrada en familias agricultoras cuya principal fuente de sustento es el cultivo de plátano, y que encuentran en esta iniciativa una oportunidad de fortalecer sus capacidades productivas a pesar de los rezagos en formación académica formal.

```{r}
#| label: alfabetismo
#| fig-cap: Sabe leer y escribir
ggplot(survey) +
  aes(x = alfabetismo) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1)
  ) +
  labs(y = "Frecuencia")
```


```{r}
#| label: educacion
#| fig-cap: Nivel educativo más alto alcanzado
ggplot(survey) +
  aes(y = educacion) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent_h() +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1)
  ) +
  labs(x = "Frecuencia", y = NULL)
```

## Situaciones de discapacidad

Ninguno de los beneficiarios encuestados reporta limitaciones funcionales ni condiciones de discapacidad. El 100 % indicó no presentar situaciones de discapacidad, y por lo tanto el número de limitaciones registradas es nulo en todos los casos. En Usaragá, el hecho de no registrar ninguna discapacidad puede relacionarse con la composición etaria relativamente madura, pero no envejecida, y con el hecho de que la población objetivo del proyecto se encuentra en edad productiva y en condiciones físicas que les permiten desempeñar actividades agrícolas, núcleo central de las acciones del proyecto. Aunque también, podría ser meramente un efecto del bajo tamaño de muestra, en la medida en que el proyecto es pequeño y solamente se propone beneficiar de manera directa a 10 familias.


```{r}
#| label: discapacidad_any
ggplot(survey) +
  aes(x = discapacidad_any) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia")
```

```{r}
#| label: discapacidad_cnt
ggplot(survey) +
  aes(x = discapacidad_cnt) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia")
```

```{r}
#| label: discapacidad_which
dicapacidad_vars <- c(
  "oir",
  "hablar",
  "ver",
  "moverse",
  "manos",
  "cognitiva",
  "autocuidado",
  "interaccion"
)
dicapacidad_dummy_vars <- paste0(dicapacidad_vars, "_dummy")
labelsvars <- purrr::map_chr(dicapacidad_vars, \(x) attr(survey[[x]], "label"))
rename_map <- setNames(dicapacidad_dummy_vars, labelsvars)
survey |>
  select(any_of(dicapacidad_dummy_vars)) |>
  rename(!!!rename_map) |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  ggplot(aes(y = name, x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1),
    labels = \(x) stringr::str_wrap(x, 20)
  ) +
  labs(x = "Frecuencia", y = "Tipo de discapacidad")
```

```{r}
#| label: ver
ggplot(survey) +
  aes(x = ver) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia")
```


## Afectaciones por el conflicto armado

En el proyecto Usaragá la gran mayoría de participantes (88,9%) ha vivido algún tipo de afectación por el conflicto armado, mientras que solo un 11,1% manifiesta no haberla experimentado. Al desagregar por tipo de afectación, se observa que la totalidad de los casos corresponde al desplazamiento forzado, lo cual refleja un patrón homogéneo en la experiencia del conflicto dentro de esta población. Este hallazgo no solo permite dimensionar la magnitud de la victimización, sino que también ayuda a entender la alta vulnerabilidad social y económica que atraviesa el grupo, aspecto que debe considerarse de manera central en la valoración del proyecto.

```{r}
#| label: afectacion_conflicto_any
#| fig-cap: Afectación por el conflicto armado (alguna)
survey$afectacion_conflicto |>
  checkbox_to_dummies() |>
  dummies_to_any() |>
  tibble(x = _) |>
  ggplot(aes(x = x)) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia", x = "Ha vivido alguna afectación por el conflicto armado")
```

```{r}
#| label: afectacion_conflicto_which
#| fig-cap: Tipo de afectación por el conflicto armado
survey$afectacion_conflicto |>
  checkbox_to_dummies() |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  ggplot(aes(y = name |> forcats::fct_reorder(value), x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(drop = FALSE) +
  labs(x = "Frecuencia", y = "Tipo de afectación por el conflicto armado")
```

## Seguridad social

En términos de la situación de afiliación al sistema de protección social de los beneficiarios del proyecto Usaragá, en primer lugar, se observa que el 100% de las personas encuestadas se encuentran afiliadas al sistema de salud a través de una EPS (del Règimen Subsidiado). Sin embargo, la cobertura en otros componentes del sistema es limitada: solo el 11,1% cotiza a pensiones y el mismo porcentaje está afiliado a una Administradora de Riesgos Laborales (ARL), mientras que la gran mayoría no cuenta con estas protecciones. Esta disparidad evidencia un patrón común en estas comunidades rurales y de base comunitaria, donde la afiliación a salud suele tener altos niveles de cobertura por el alcance que tiene el Régimen Subsidiado, pero la vinculación al régimen de pensiones y riesgos laborales es baja, asociada a condiciones de informalidad laboral.

```{r}
#| label: salud
#| fig-cap: Salud
ggplot(survey) +
  aes(x = afiliado_salud) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```

```{r}
#| label: pension
#| fig-cap: Pensión
ggplot(survey) +
  aes(x = cotiza_pension) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```

```{r}
#| label: riesgos
#| fig-cap: Riesgos
ggplot(survey) +
  aes(x = afiliado_riesgos_laborales) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```


## Ocupación

En esta sección se presenta la caracterización la participación laboral de las personas encuestadas. En la primera se observa que el 100% de los participantes se encuentran ocupados, lo que refleja una inserción plena en actividades productivas. La segunda gráfica muestra que la ocupación principal se concentra en la agricultura, aunque también se reportan actividades complementarias como agricultura combinada con pesca, pesca independiente y oficios domésticos y de aseo (cada uno con 11,1%). Finalmente, la tercera gráfica señala que un 33,3% de las personas diversifica sus ingresos a través de otros trabajos o negocios, mientras que el 66,7% depende exclusivamente de su actividad principal. Estos resultados evidencian, por una parte, el resultado de los criterios de selección de los beenficiarios del proyecto, pero también la centralidad de la agricultura en la economía local.

```{r}
#| label: mcdo_laboral
#| fig-cap: Participación en el mercado laboral
ggplot(survey) +
  aes(x = mcdo_laboral) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```

```{r}
#| label: ocupacion
#| fig-cap: Ocupación
survey |>
  filter(!is.na(ocupacion)) |>
  ggplot(aes(y = ocupacion |> forcats::fct_infreq() |> forcats::fct_rev())) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7, na.rm = TRUE) +
  geom_percent_h() +
  labs(y = "Ocupación", x = "Frecuencia")
```

```{r}
#| label: otros_trabajos
#| fig-cap: Participación en el mercado laboral
ggplot(survey) +
  aes(x = otros_trabajos) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```

## Apoyos gubernamentales y no gubernamentales

En el proyecto Usaragá ninguno de los participantes recibe ayudas o subsidios de entidades gubernamentales, lo que evidencia una ausencia total de acceso a programas sociales estatales como Colombia Mayor, Ingreso Solidario o Familias en Acción. Del mismo modo, tampoco se registran apoyos provenientes de entidades no gubernamentales. Este hallazgo refleja que la población vinculada al proyecto enfrenta sus condiciones de vida sin respaldo institucional directo, lo que contrasta con otros territorios del Pacífico donde los subsidios son una fuente complementaria de ingresos o de oportunidades o capacidades. La falta de acceso a este tipo de programas resalta la importancia del proyecto como un espacio que busca fortalecer las capacidades productivas y de organización comunitaria, dado que la comunidad no cuenta con apoyos externos para amortiguar su vulnerabilidad económica.


```{r}
#| label: ayudas_gobierno_any
#| fig-cap: Recibió durante los últimos 12 meses alguna ayuda o subsidio de entidades gubernamentales
survey$ayudas_gobierno |>
  checkbox_to_dummies() |>
  dummies_to_any() |>
  tibble(x = _) |>
  ggplot(aes(x = x)) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia", x = "Recibe ayudas o subsidios de entidades gubernamentales")
```

```{r}
#| label: ayudas_gobierno_which
#| fig-cap: Programas de los cuales recibió ayudas o subsidios
survey$ayudas_gobierno |>
  checkbox_to_dummies() |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  ggplot(aes(y = name |> forcats::fct_reorder(value), x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(drop = FALSE) +
  labs(x = "Frecuencia", y = "Programas de los cuales recibió ayudas o subsidios")
```


```{r}
#| label: ayudas_no_gubernamentales
#| fig-cap: Recibió durante los últimos 12 meses alguna ayuda o subsidio de entidades no gubernamentales
ggplot(survey) +
  aes(x = ayudas_no_gubernamentales) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  labs(y = "Frecuencia")
```


## Participación comunitaria

Las siguientes gráficas del proyecto Usaragá muestran los niveles de participación comunitaria de la población encuestada. La primera evidencia que todas las personas participantes (100 %) pertenecen a alguna organización, lo que refleja un fuerte vínculo comunitario. La mayoría está vinculada a organizaciones comunitarias como los consejos comunitarios, y en un porcentaje relevante (44,4 %) a asociaciones campesinas o agropecuarias. Asimismo, casi la totalidad de los encuestados (88,9 %) tuvo contacto con alguna organización en el último año, lo que indica una participación activa y sostenida en la vida colectiva. Además, más de la mitad (55,6 %) desempeña roles de liderazgo, especialmente en organizaciones comunitarias (33,3 %) y campesinas/agropecuarias (22,2 %). En conjunto, estos hallazgos reflejan que la población de Usaragá no solo está organizada, sino que mantiene una participación activa en instancias comunitarias y agrícolas, con una presencia importante de liderazgos locales, lo que resulta coherente con la definición de la población objetivo del proyecto, que reconoce la centralidad de las organizaciones en la gestión comunitaria y productiva del territorio.


```{r}
#| label: pertenencia_organizaciones_any
#| fig-cap: Pertenece a alguna organización
survey$pertenencia_organizaciones |>
  checkbox_to_dummies() |>
  dummies_to_any() |>
  tibble(x = _) |>
  ggplot(aes(x = x)) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia", x = "Pertenece a alguna organización")
```

```{r}
#| label: pertenencia_organizaciones_which
#| fig-cap: Organizaciones a las que pertenece
survey$pertenencia_organizaciones |>
  checkbox_to_dummies() |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  filter(value > 0) |>
  ggplot(aes(y = name |> forcats::fct_reorder(value), x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1),
    labels = \(x) x |>
      stringr::str_trunc(50) |>
      stringr::str_wrap(25)
  ) +
  # scale_x_continuous(expand = expansion(0, 1))+
  labs(x = "Frecuencia", y = "Organizaciones a las que pertenece")
```




```{r}
#| label: contacto_organizaciones_any
#| fig-cap: En el último año, tuvo contacto con alguna organización
survey$contacto_organizaciones |>
  checkbox_to_dummies() |>
  dummies_to_any() |>
  tibble(x = _) |>
  ggplot(aes(x = x)) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia", x = "En el último año, tuvo contacto con alguna organización")
```

```{r}
#| label: contacto_organizaciones_which
#| fig-cap: Organizaciones con las que tuvo contacto el último año
survey$contacto_organizaciones |>
  checkbox_to_dummies() |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  filter(value > 0) |>
  ggplot(aes(y = name |> forcats::fct_reorder(value), x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1),
    labels = \(x) x |>
      stringr::str_trunc(50) |>
      stringr::str_wrap(25)
  ) +
  labs(x = "Frecuencia", y = "Organizaciones a las que pertenece")
```



```{r}
#| label: liderazgo_organizaciones_any
#| fig-cap: Lidera organizaciones
survey$liderazgo_organizaciones |>
  checkbox_to_dummies() |>
  dummies_to_any() |>
  tibble(x = _) |>
  ggplot(aes(x = x)) +
  geom_bar(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_percent() +
  scale_fill_discrete(drop = FALSE) +
  scale_x_discrete(drop = FALSE) +
  labs(y = "Frecuencia", x = "Lidera organizaciones")
```

```{r}
#| label: liderazgo_organizaciones_which
#| fig-cap: Organizaciones que lidera
survey$liderazgo_organizaciones |>
  checkbox_to_dummies() |>
  mutate(tot = 1) |>
  summarise(across(everything(), sum)) |>
  pivot_longer(-tot) |>
  mutate(perc = value / tot) |>
  filter(value > 0) |>
  ggplot(aes(y = name |> forcats::fct_reorder(value), x = value)) +
  geom_col(fill = "steelblue", color = "white", alpha = 0.7) +
  geom_text(
    aes(
      label = scales::percent(perc, 0.1)
    ),
    hjust = "inward",
    fontface = "bold"
  ) +
  scale_fill_discrete(drop = FALSE) +
  scale_y_discrete(
    drop = FALSE,
    guide = guide_axis(check.overlap = TRUE, n.dodge = 1),
    labels = \(x) x |>
      stringr::str_trunc(50) |>
      stringr::str_wrap(25)
  ) +
  labs(x = "Frecuencia", y = "Organizaciones que lidera")
```

## Síntesis de la caracterización

En síntesis, la población vinculada al proyecto Usaragá se caracteriza por ser homogéneamente afrodescendiente, con niveles educativos concentrados en la básica primaria, alta alfabetización y plena vinculación laboral, principalmente en la agricultura y actividades asociadas. Se trata de una comunidad que no recibe subsidios ni apoyos externos, pero que mantiene una fuerte organización social y liderazgo comunitario, en particular en asociaciones campesinas y consejos comunitarios. Además, gran parte de la población ha sido afectada por el conflicto armado, especialmente mediante el desplazamiento forzado, lo que marca su trayectoria colectiva y refuerza la importancia de la organización como mecanismo de resiliencia. Estos elementos coinciden con la definición de la población objetivo del proyecto, que reconoce a comunidades afrodescendientes rurales, socialmente activas y organizadas en torno a la producción agrícola, como actores centrales para el fortalecimiento de sus capacidades y su proyección territorial.
